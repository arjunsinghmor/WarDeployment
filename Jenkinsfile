pipeline {
  agent any
    tools {
        maven '3.6.3'
    }
    stages {
        stage('Build') {
            steps {
                echo 'Artifacts pushed to bucket for deployment'
            }
        }
    stage('Publish') {
            steps {
               echo 'Artifacts pushed to bucket for deployment'
            }
        }
		stage('Deploy') {
            steps {
                echo 'Artifacts pushed to bucket for deployment'
            }
        }
    }
}
